# Specify the minimum required CMake version
cmake_minimum_required(VERSION 3.10)

# Define the project name
project(traffic_sniffer)

# Add an executable target
add_executable(traffic_sniffer main.c)